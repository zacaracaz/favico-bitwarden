# Shell scripts must be LF or the shebang breaks on macOS/Linux.
*.sh    text eol=lf
# Node scripts: normalize to LF.
*.mjs   text eol=lf
*.js    text eol=lf
# Windows batch must be CRLF.
*.cmd   text eol=crlf
*.bat   text eol=crlf
